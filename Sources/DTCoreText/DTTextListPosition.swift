import Foundation

/// Marker position for a `DTTextList` — mirrors the CSS `list-style-position` property.
public enum DTTextListPosition: Int {
  case outside = 0
  case inside = 1
  case inherit = 2
}
